pageSettings: &pageSettings
  label: 'Page Settings'
  name: 'pageSettings'
  collapsed: true
  widget: 'object'
  fields:
    - label: 'Language'
      name: 'language'
      widget: 'select'
      options: ['pt-BR', 'en', 'es']
      default: 'pt-BR'
      collapsed: true
    - label: 'Slug PT-BR'
      name: 'link_pt_br'
      widget: 'string'
      required: false
      default: '#'
      collapsed: true
    - label: 'Slug EN'
      name: 'link_en'
      widget: 'string'
      required: false
      default: '#'
      collapsed: true
    - label: 'Slug ES'
      name: 'link_es'
      widget: 'string'
      required: false
      default: '#'
      collapsed: true
    - label: 'Animations'
      name: 'animations'
      widget: 'select'
      options: 
        - { label: 'Enable All', value: 'enable_all' }
        - { label: 'Disable All', value: 'disable_all' }
        - { label: 'Custom by Components', value: 'custom' }
      default: 'enable_all'
      required: true
      hint: 'Control animation behavior for the entire page'
      collapsed: true
    - label: 'Text Direction'
      name: 'direction'
      widget: 'select'
      options: ['left', 'right']
      default: 'left'
      collapsed: true
    - label: 'SEO Title'
      name: 'seoTitle'
      widget: 'string'
      required: false
      hint: 'title shown in search results and in link previews on social media'
      collapsed: true
    - label: 'SEO Description'
      name: 'seoDescription'
      widget: 'string'
      required: false      
      hint: 'description of the page shown in search results and in link previews on social media'
      collapsed: true
    - label: 'SEO Keywords'
      name: 'seoKeywords'
      widget: 'list'
      required: false
      hint: 'keywords that help search engines understand the page topic'
      collapsed: true
      fields:
        - { label: 'Keyword', name: 'keyword', widget: 'string', collapsed: true }
    - label: 'SEO Image'
      name: 'seoImage'
      widget: 'image'
      required: false
      hint: 'image shown in link previews on social media'
      collapsed: true
